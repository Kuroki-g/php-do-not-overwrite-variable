{
  // This file is for the root project.
  // for build, use each project's tsconfig.json
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "out",
    "rootDir": "src",
  },
  "include": ["src"],
  "references": [{ "path": "./client" }, { "path": "./server" }]
}
